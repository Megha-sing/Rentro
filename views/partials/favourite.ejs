<% if(isLoggedIn && user.userType === 'guest') { %>
    <form action="/favourites" method="POST">
        <button type="submit" class="border border-gray-600 shadow hover:shadow-gray-500 text-pink-500 px-4 py-2 rounded-4xl hover:cursor-pointer transition-all">Add To Favourite</button>
        <input type="hidden" name="id" value="<%= home._id %>"/>
    </form>
<% } %>